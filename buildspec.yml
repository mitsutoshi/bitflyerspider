version: 0.2

phases:
    install:
        commands:
            - go get -u github.com/golang/dep/cmd/dep
    pre_build:
        commands:
            - dep ensure -update
    build:
        commands:
            - go test -v ./tests
            - make cross-build
artifacts:
    files:
        - bin/**/*
